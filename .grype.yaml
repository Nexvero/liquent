# The former three-entry CPython 3.13.14 exception expired on 2026-08-31.
# Python 3.13.15 is now the pinned stable base image. Risk acceptance never
# transfers across runtime versions, so the replacement starts with no ignores.
ignore: []
