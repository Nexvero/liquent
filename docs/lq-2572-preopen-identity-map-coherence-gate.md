# LQ-2572 Preopen identity-map coherence gate

- Workspace identity, key types, fixed names, values, and uniqueness form one gate.
- Every condition succeeds before the first workspace descriptor is opened.
- One invalid mapping fact prevents all filesystem observation.
- No partially coherent subset proceeds into child verification.
- The source mapping remains unchanged and externally owned.
- Failure uses only the existing detail-limited controlled rejection.
