# LQ-1287 Joint engine API layout budget contract

- Every supported source-layout generation has the same aggregate ceiling.
- The legacy policy-bound layout contains ten canonical sources.
- The image-bound layout contains eleven canonical sources.
- The run-bound layout contains fourteen canonical sources.
- Layout-specific authority material consumes the shared total budget.
- No generation receives an implicit compatibility exemption.
- Exact-name inventory and per-source limits remain generation-specific.
