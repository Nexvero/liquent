# LQ-2506 Postphase bound directory-set gate

- Optional mapped output capture precedes the postphase identity-map check.
- The resulting mapping is the complete expected intermediate workspace state.
- Previously retained and newly captured directories are checked together.
- A private same-name replacement remains a continuity failure.
- Receipt parsing starts only after the bound directory set is verified.
- Rejection leaves no successful controlled evidence or public result.
