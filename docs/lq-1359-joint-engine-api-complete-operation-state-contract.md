# LQ-1359 Joint engine API complete operation state contract

- Operation resolution binds root identity and full stable root metadata.
- It binds each fixed child's identity and full stable child metadata.
- Initial and final real component chains must reach the same state.
- Root and child continuity are jointly necessary for success.
- No valid subset can compensate for mutation elsewhere in the boundary.
- The immutable result exposes only the identities needed later.
- Later validation still resolves a fresh complete current boundary.
