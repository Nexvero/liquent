# LQ-1299 Joint engine API bounded layout composition contract

- Every supported layout composes individual and aggregate source limits.
- Ten-source policy roots retain their canonical source sequence.
- Eleven-source image roots count image authority before that sequence.
- Fourteen-source run roots count all run and image authority first.
- Each additional authority value consumes the same remaining allowance.
- No legacy generation bypasses pre-read or exhaustion enforcement.
- Snapshot decoding and cryptographic verification contracts are unchanged.
