# LQ-1825 Joint engine API accepted registry context evidence

- Tests construct a valid registry-bound accepted result.
- Empty and duplicate-marker contexts are rejected.
- An unrelated-only context is rejected.
- Stable audits observe three equal registry contexts.
- Injected terminal context divergence is rejected.
- Existing accepted audit contracts remain green.
- All focused warnings are treated as errors.
