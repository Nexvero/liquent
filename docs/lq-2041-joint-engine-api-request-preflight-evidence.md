# LQ-2041 Joint engine API request preflight evidence

- Tests cover every invalid root shape.
- Tests cover every non-boolean mode category.
- Accept proves no clock or root-resolution call.
- Both Audit modes prove the same ordering.
- Invalid mode proves no root validation call.
- Valid request behavior remains unchanged.
- Strict warnings remain enabled.
