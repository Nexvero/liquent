# LQ-1311 Joint engine API layout path binding contract

- Path identity revalidation applies to every supported source generation.
- Ten-source policy-bound roots receive no compatibility exemption.
- Eleven-source image-bound roots use the same final identity gate.
- Fourteen-source run-bound roots use the same final identity gate.
- Layout-specific names affect inventory, not pathname trust semantics.
- Every successful layout preserves one continuously visible root identity.
- No generation discovery or automatic fallback is added.
