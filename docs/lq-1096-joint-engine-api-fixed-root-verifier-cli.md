# LQ-1096 — Joint Engine API Fixed-root Verifier CLI

## Ergebnis

Akzeptiert ausschließlich `--source-root` und liefert detailfrei Exit 0 oder 2.

## Grenze

Die CLI ist read-only und erzeugt oder verändert keine Quelle.
