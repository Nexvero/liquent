# LQ-027 — Visual Preview Stabilization and Release Checkpoint

## Status

- Phase 2 implemented.
- Local visual preview checkpoint documented.
- No code changes.
- No dependency changes.
- No live trading.
- No trading recommendation.
- No profitability assessment.

## 1. Purpose

- Dieses Dokument markiert den aktuellen Visual-Preview-Stand als stabilen
  lokalen Checkpoint.
- Es dient als Referenz vor neuen Feature-Phasen.
- Es ist **kein** Produktrelease.
- Es ist **keine** Trading-Anleitung.

## 2. Current Scope

### Runtime/Start
- optionales Streamlit-Extra `visual`
- Fallback ohne Streamlit: `python -m tools.visual_preview.app`
- Start mit Streamlit: `streamlit run tools/visual_preview/app.py`

### Data Sources
- Synthetic datasets: `micro_long`, `micro_short`, `stair_cooldown`
- Local CSV upload in-memory
- Bid/Ask CSV: `timestamp,bid,ask[,volume]`
- OHLCV CSV: `timestamp,open,high,low,close[,volume]`;
  `close -> bid`, `close -> ask`, `mid = close`

### Strategy Preview
- v0, v1
- v1 parameters: `breakout_threshold_pct`, `cooldown_bars`, `max_signals_per_day`
- signal-only preview: `generate_signals`; **no runner**; **no equity**

### UI
- Safety Banner
- Technical Summary
- Mid Chart
- Signal Table
- Strategy Metadata
- CSV Format Help
- Sample CSV Codeblocks

### Documentation
- README Quickstart
- `docs/lq-025-visual-preview-quickstart.md`
- `docs/visual-preview-index.md`
- LQ-019..LQ-026 Specs

## 3. Smoke Checks

```bash
git status --short
. .venv/bin/activate
python -m pytest
python - <<'PY'
import tools.visual_preview.app as app
print("import ok", hasattr(app, "main"))
PY
python -m tools.visual_preview.app || true
git status --short | grep -i '\.csv' && echo "UNEXPECTED CSV FILE IN STATUS" || true
```

Erwartung:
- Working Tree clean
- pytest grün
- import ok True
- fallback ohne Traceback
- keine CSV-Dateien im Status

Optional, nur wenn Streamlit manuell installiert wurde:

```bash
streamlit run tools/visual_preview/app.py
```

Manuell prüfen: Safety Banner sichtbar · Synthetic dataset auswählbar · Strategy
v0/v1 auswählbar · Technical Summary sichtbar · Mid Chart sichtbar · Signal Table
sichtbar · CSV-Modus zeigt Bid/Ask und OHLCV Beispiele.

> Wichtig: **keine** Installation in diesem Checkpoint; Streamlit-Start bleibt
> optional.

## 4. Release Criteria

- `main` synchron mit `origin/main`
- Working Tree clean
- pytest grün
- app importiert ohne Streamlit
- fallback ohne Streamlit stabil
- keine echten CSV-Dateien im Status
- keine Reports im Status
- README Quickstart vorhanden
- Visual Preview Index vorhanden
- Quickstart-Doku vorhanden
- Safety-Grenzen dokumentiert
- keine Live-/Paper-/Exchange/API-Pfade
- keine Profitabilitäts-/Empfehlungssprache außerhalb Safety-Disclaimern

## 5. Known Boundaries

- Streamlit ist optional und nicht automatisch installiert.
- keine produktive Web-App; kein Deployment; kein Auth/Login.
- kein Persistieren von CSV-Uploads.
- keine automatische Datenbeschaffung; keine Live-Daten.
- keine Runner-/Backtest-Equity-Integration in der Preview.
- kein Kostenmodell in der Visual Preview.
- kein Export/Report aus der Preview.
- keine Screenshots.
- kein Chart-Library-Ausbau über Streamlit-native Funktionen hinaus.

## 6. Safety Boundaries

- No API keys.
- No exchange credentials.
- No network calls by Liquent.
- No external data download by Liquent.
- No live data source.
- No orders.
- No paper-trading connection.
- Uploaded CSV files are not saved by Liquent.
- No real CSV files committed.
- No report files generated by the preview.
- No profitability assessment.
- No trading recommendation.
- No equity/performance display as decision basis.

## 7. Recommended Next Development Paths

### Option A — Stop and stabilize
keine neuen Features; nur Bugfixes/Doku. Sinnvoll, wenn der lokale
Visual-Preview-Checkpoint genügt.

### Option B — Manual UI smoke test with optional Streamlit
Streamlit optional installieren/starten; manuell prüfen; **keine** Screenshots
committen.

### Option C — Local runner integration specification
nur mit neuer Spezifikation; Ziel wäre, später technische Runner-Kennzahlen
sichtbar zu machen — **ohne** Equity-/Profitabilitätsfokus.

### Option D — Cost model display in Visual Preview
nur technische Anzeige/Parameter; **kein** Performance-Fokus; nur mit neuer
Spezifikation.

> **Empfehlung:** LQ-027 bleibt Doku-only und markiert den stabilen Checkpoint.
> Danach optional LQ-028 Phase 1 — controlled local Streamlit smoke-test
> checklist. Alternativ Pause/Review, bevor Runner/CostModel in der UI diskutiert
> werden.

## 8. Phase 2 Implementation Status

- Checkpoint-Doku finalisiert (diese Datei).
- README-Link ergänzt.
- Visual Preview Index-Link ergänzt (LQ-027 in der Evolution-Tabelle).
- Roadmap-Link ergänzt.
- Doku-Tests ergänzt (`tests/test_visual_preview_stabilization_checkpoint.py`).
- Keine Codeänderung, keine tools-Änderung, keine src-Änderung, keine
  pyproject-Änderung.
- Keine Dependency installiert, keine Echtdaten, keine CSV-Dateien, keine Reports.
- pytest: alle Tests grün (siehe README-Teststand).

---

*Research-/Backtesting-Kontext. Keine Live-/Paper-Trading-Funktion, keine
Exchange-Anbindung, keine Profitabilitätsaussage, keine Handelsempfehlung.*
