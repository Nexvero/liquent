# LQ-2592 Cumulative PostgreSQL required-suite evidence

- The complete suite ran with `LIQUENT_REQUIRE_POSTGRES_TESTS=1`.
- PostgreSQL fixtures created and dropped independent throwaway databases.
- An initial sandbox-only run could not reach local TCP and was not evidence.
- The authorized external run exposed twelve real failures for correction.
- UTC server sessions resolved eight persistence reconstruction failures.
- Four roadmap inventory assertions were synchronized with current facts.
- The focused correction run passed all 25 affected tests.
- The final complete required run passed all 7277 tests without skips.
- PostgreSQL version was 16.14 and session timezone was UTC.
- This local evidence does not authorize staging or production deployment.
