# LQ-2164 Detail-free joint engine API shared root text rejection

- Rejection reveals no rendered text.
- It reveals no Unicode category.
- It reveals no byte or component length.
- Direct API exposes only unavailable failure.
- CLI exposes status two only.
- Stdout and stderr remain empty.
- No diagnostic fallback exists.
