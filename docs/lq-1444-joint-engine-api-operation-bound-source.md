# LQ-1444 Joint engine API operation-bound source

- Operation accept passes the resolved source identity to one-shot verify.
- It also continues passing the separately resolved acceptance identity.
- The one-shot loader rejects a source replacement before verification.
- Equal copied content does not override descriptor identity mismatch.
- The outer finally path still revalidates the operation-root graph.
- The established unavailable boundary normalizes technical failures.
- No caller-controlled identity, role, or allow decision is accepted.
