# LQ-1477 Joint engine API marker generation evidence

- Tests replace a marker after the first verified observation.
- Replacement preserves canonical bytes and owner-private mode.
- The new inode causes final observation inequality and rejection.
- Stable marker generation continues to allow the audit lifecycle.
- Existing content, mode, link, and absence mutation tests remain.
- Root identity is passed to both observation calls.
- Strict warning treatment guards compatibility regressions.
