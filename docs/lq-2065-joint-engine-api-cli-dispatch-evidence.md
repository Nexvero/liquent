# LQ-2065 Joint engine API CLI dispatch evidence

- Tests prove every fixed mode mapping.
- Tests prove exactly one operation call.
- Tests cover invalid type and string modes.
- Tests cover false-like foreign completions.
- Tests cover arbitrary payload completion.
- Main proves silent status two for every mode.
- Strict warnings remain enabled.
