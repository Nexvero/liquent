# LQ-1132 — Joint Engine API Run-bound Root Verifier

## Ergebnis

Komponiert vierzehn descriptor-relative Reads, interne UTC-Zeit und beide Signaturprüfungen.

## Grenze

Die CLI akzeptiert ausschließlich die feste Quellenwurzel.
