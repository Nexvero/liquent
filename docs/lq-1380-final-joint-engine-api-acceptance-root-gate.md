# LQ-1380 Final joint engine API acceptance root gate

- One helper re-opens the visible root through all real components.
- It compares stable security facts with the held working descriptor.
- Both descriptors must remain owner-private and non-inheritable.
- The revalidation descriptor closes before the helper returns.
- Load invokes the gate after its single marker decision.
- Inspection invokes it after its stable inventory snapshot.
- Record invokes it after durable file and directory synchronization.
