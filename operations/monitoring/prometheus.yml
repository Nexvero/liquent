global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: liquent-control-plane
    metrics_path: /internal/metrics
    static_configs:
      - targets: ["control-plane:8000"]
