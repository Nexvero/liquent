name: liquent

# LQ-057 deployment contract, with the LQ-301 controlled research-worker
# wiring. Production image variables must contain
# immutable digest references (repository@sha256:...).

x-app-base: &app-base
  image: ${LIQUENT_APP_IMAGE:?set an immutable application image digest}
  restart: unless-stopped
  init: true
  read_only: true
  security_opt:
    - no-new-privileges:true
  cap_drop:
    - ALL
  tmpfs:
    - /tmp:size=64m,mode=1777
  env_file:
    - path: ./runtime.env
      required: true
  secrets:
    - database_url
  networks:
    - application
  logging:
    driver: json-file
    options:
      max-size: 10m
      max-file: "5"

services:
  migration-gate:
    <<: *app-base
    command: ["liquent-migrate"]
    restart: "no"
    depends_on:
      postgres:
        condition: service_healthy
    cpus: 0.50
    mem_limit: 512m

  control-plane:
    <<: *app-base
    command: ["liquent-control-plane"]
    expose:
      - "8000"
    networks:
      - public
      - application
      - observability
    depends_on:
      migration-gate:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health/ready', timeout=2)"]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 10s
    cpus: 1.50
    mem_limit: 1536m

  research-worker:
    <<: *app-base
    command:
      - liquent-research-worker
      - --configuration
      - /run/liquent/research-worker.json
      - --database-url-file
      - /run/secrets/database_url
    secrets:
      - source: database_url
        target: database_url
        uid: "10001"
        gid: "10001"
        mode: 0400
    stop_grace_period: 60s
    networks:
      - application
      - data
      - observability
    depends_on:
      migration-gate:
        condition: service_completed_successfully
    cpus: 3.00
    mem_limit: 4096m
    volumes:
      - ${LIQUENT_RESEARCH_WORKER_CONFIG_FILE:?set owner-only worker configuration file}:/run/liquent/research-worker.json:ro
      - ${LIQUENT_RESEARCH_WORKER_ID_FILE:?set owner-only stable worker ID file}:/run/liquent/research-worker-id:ro
      - ${LIQUENT_RESEARCH_DATA_ROOT:?set read-only research data directory}:/var/lib/liquent/research-data:ro
      - artifacts:/var/lib/liquent/artifacts

  postgres:
    image: ${LIQUENT_POSTGRES_IMAGE:?set an immutable PostgreSQL image digest}
    restart: unless-stopped
    init: true
    shm_size: 256m
    environment:
      POSTGRES_DB: liquent
      POSTGRES_USER: liquent
      POSTGRES_PASSWORD_FILE: /run/secrets/postgres_password
    secrets:
      - postgres_password
    networks:
      - application
      - data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U liquent -d liquent"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 20s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    cpus: 1.50
    mem_limit: 3072m
    volumes:
      - postgres-data:/var/lib/postgresql/data
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "5"

  prometheus:
    image: ${LIQUENT_PROMETHEUS_IMAGE:?set an immutable Prometheus image digest}
    restart: unless-stopped
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    networks:
      - observability
    cpus: 0.35
    mem_limit: 512m
    volumes:
      - ../monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus

  grafana:
    image: ${LIQUENT_GRAFANA_IMAGE:?set an immutable Grafana image digest}
    restart: unless-stopped
    read_only: true
    environment:
      GF_SECURITY_ADMIN_PASSWORD__FILE: /run/secrets/grafana_admin_password
      GF_SERVER_HTTP_ADDR: 0.0.0.0
    secrets:
      - grafana_admin_password
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    networks:
      - observability
    cpus: 0.35
    mem_limit: 512m
    tmpfs:
      - /tmp:size=32m,mode=1777
    volumes:
      - grafana-data:/var/lib/grafana

networks:
  public:
    external: true
    name: liquent_public
  application:
    external: true
    name: liquent_application
  data:
    external: true
    name: liquent_data
  observability:
    external: true
    name: liquent_observability

volumes:
  postgres-data:
    name: liquent_postgres_data
  artifacts:
    name: liquent_artifacts
  prometheus-data:
    name: liquent_prometheus_data
  grafana-data:
    name: liquent_grafana_data

secrets:
  database_url:
    file: ${LIQUENT_SECRETS_DIR:?set host secrets directory}/database_url
  postgres_password:
    file: ${LIQUENT_SECRETS_DIR:?set host secrets directory}/postgres_password
  grafana_admin_password:
    file: ${LIQUENT_SECRETS_DIR:?set host secrets directory}/grafana_admin_password
