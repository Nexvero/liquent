[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "liquent"
version = "0.0.1"
description = "Liquent — understand liquidity. Modular skeleton (analysis mode only, no live execution)."
readme = "README.md"
requires-python = ">=3.10"
license = "LicenseRef-Proprietary"
authors = [{ name = "Liquent Team" }]
# LQ-058: minimal control-plane runtime only. These packages provide HTTP,
# typed settings, and the ASGI server; no trading connectivity is included.
# LQ-161: PyJWT[crypto] and httpx2 are the verified base for the later OIDC
# verification adapter (LQ-160). Neither is used by production code yet.
dependencies = [
    "alembic>=1.16,<2",
    "fastapi>=0.115,<1",
    "httpx2>=2,<3",
    "prometheus-client>=0.22,<1",
    "psycopg[binary]>=3.2,<4",
    "pydantic-settings>=2.7,<3",
    "PyJWT[crypto]>=2.13,<3",
    "sqlalchemy>=2.0,<2.1",
    "uvicorn>=0.34,<1",
]

[project.scripts]
liquent-control-plane = "liquent_platform.transport.http.main:main"
liquent-migrate = "liquent_platform.persistence.migrate:main"
liquent-health-check = "liquent_platform.observability.external_health:main"
liquent-oidc-trust = "liquent_platform.operators.oidc_trust:main"
liquent-initial-bootstrap = "liquent_platform.operators.initial_bootstrap:main"
liquent-membership-management = "liquent_platform.operators.membership_management:main"
liquent-oidc-trust-authority = "liquent_platform.operators.oidc_trust_authority:main"
liquent-membership-authority = "liquent_platform.operators.membership_authority:main"
liquent-oidc-trust-authority-recovery = "liquent_platform.operators.oidc_trust_authority_recovery:main"
liquent-membership-authority-recovery = "liquent_platform.operators.membership_authority_recovery:main"
liquent-user-lifecycle = "liquent_platform.operators.user_lifecycle:main"
liquent-workspace-lifecycle = "liquent_platform.operators.workspace_lifecycle:main"
liquent-release-signing = "liquent_platform.operators.release_signing:main"
liquent-release-promotion = "liquent_platform.operators.release_promotion:main"
liquent-release-publication = "liquent_platform.operators.release_publication:main"
liquent-release-registry-bootstrap = "liquent_platform.operators.release_registry_bootstrap:main"
liquent-release-key-activation = "liquent_platform.operators.release_key_activation:main"
liquent-release-publication-bootstrap = "liquent_platform.operators.release_publication_bootstrap:main"
liquent-release-publication-executor = "liquent_platform.operators.release_publication_executor:main"
liquent-release-publication-handoff = "liquent_platform.operators.release_publication_handoff:main"
liquent-research-worker = "liquent_platform.operators.research_worker:main"
liquent-research-worker-staging-evidence = "liquent_platform.operators.research_worker_staging_evidence:main"
liquent-staging-phase-probe = "liquent_platform.operators.staging_read_only_probe_cli:main"
liquent-runtime-inspect = "liquent_platform.operators.runtime_inspect:main"
liquent-artifact-capability-inspect = "liquent_platform.operators.artifact_capability_inspect:main"
liquent-artifact-probe-recovery-inspect = "liquent_platform.operators.artifact_probe_recovery_inspect:main"
liquent-artifact-probe-recovery-remove = "liquent_platform.operators.artifact_probe_recovery_remove:main"
liquent-artifact-probe-recovery = "liquent_platform.operators.artifact_probe_recovery:main"
liquent-artifact-probe-recovery-reconcile = "liquent_platform.operators.artifact_probe_recovery_reconcile:main"
liquent-rollback-evidence-inspect = "liquent_platform.operators.rollback_evidence_inspect:main"
liquent-disposable-postgres-reconcile = "liquent_platform.operators.disposable_postgres_reconcile:main"
liquent-disposable-postgres-claim-reconcile = "liquent_platform.operators.disposable_postgres_claim_reconcile:main"
liquent-disposable-postgres-disposition = "liquent_platform.operators.disposable_postgres_disposition:main"
liquent-disposable-postgres-cleanup-preflight = "liquent_platform.operators.disposable_postgres_cleanup_preflight:main"
liquent-disposable-postgres-runtime-cleanup = "liquent_platform.operators.disposable_postgres_runtime_cleanup:main"
liquent-disposable-postgres-cleanup-reconcile = "liquent_platform.operators.disposable_postgres_cleanup_reconcile:main"
liquent-disposable-postgres-cleanup-finalize = "liquent_platform.operators.disposable_postgres_cleanup_finalize:main"
liquent-disposable-postgres-cleanup-continue = "liquent_platform.operators.disposable_postgres_cleanup_continue:main"
liquent-disposable-postgres-cleanup-continue-reconcile = "liquent_platform.operators.disposable_postgres_cleanup_continue_reconcile:main"
liquent-disposable-postgres-cleanup-continue-finalize = "liquent_platform.operators.disposable_postgres_cleanup_continue_finalize:main"
liquent-disposable-postgres-cleanup-recontinue = "liquent_platform.operators.disposable_postgres_cleanup_recontinue:main"
liquent-disposable-postgres-cleanup-recontinue-reconcile = "liquent_platform.operators.disposable_postgres_cleanup_recontinue_reconcile:main"
liquent-disposable-postgres-cleanup-recontinue-finalize = "liquent_platform.operators.disposable_postgres_cleanup_recontinue_finalize:main"
liquent-disposable-postgres-cleanup-chain-continue = "liquent_platform.operators.disposable_postgres_cleanup_chained_continue:main"
liquent-disposable-postgres-cleanup-chain-reconcile = "liquent_platform.operators.disposable_postgres_cleanup_chained_continue_reconcile:main"
liquent-disposable-postgres-cleanup-chain-finalize = "liquent_platform.operators.disposable_postgres_cleanup_chained_continue_finalize:main"
liquent-disposable-postgres-cleanup-generation-continue = "liquent_platform.operators.disposable_postgres_cleanup_generation_continue:main"
liquent-disposable-postgres-cleanup-generation-reconcile = "liquent_platform.operators.disposable_postgres_cleanup_generation_continue_reconcile:main"
liquent-disposable-postgres-cleanup-generation-finalize = "liquent_platform.operators.disposable_postgres_cleanup_generation_continue_finalize:main"
liquent-disposable-postgres-volume-disposition = "liquent_platform.operators.disposable_postgres_volume_disposition:main"
liquent-disposable-postgres-volume-deletion-preflight = "liquent_platform.operators.disposable_postgres_volume_deletion_preflight:main"
liquent-disposable-postgres-volume-delete = "liquent_platform.operators.disposable_postgres_volume_deletion:main"
liquent-disposable-postgres-volume-delete-reconcile = "liquent_platform.operators.disposable_postgres_volume_deletion_reconcile:main"
liquent-disposable-postgres-volume-delete-finalize = "liquent_platform.operators.disposable_postgres_volume_deletion_finalize:main"
liquent-disposable-postgres-volume-delete-continue = "liquent_platform.operators.disposable_postgres_volume_deletion_continue:main"
liquent-disposable-postgres-volume-delete-continue-reconcile = "liquent_platform.operators.disposable_postgres_volume_deletion_continue_reconcile:main"
liquent-disposable-postgres-volume-delete-continue-finalize = "liquent_platform.operators.disposable_postgres_volume_deletion_continue_finalize:main"
liquent-disposable-postgres-volume-delete-terminal-handoff = "liquent_platform.operators.disposable_postgres_volume_deletion_terminal_handoff:main"
liquent-supervisor-control-directory-cleanup = "liquent_platform.operators.manifest_handoff_supervisor_control_directory_cleanup:main"
liquent-supervisor-control-directory-retire = "liquent_platform.operators.manifest_handoff_supervisor_control_directory_retirement:main"
liquent-supervisor-cleanup-authority-bootstrap = "liquent_platform.operators.manifest_handoff_supervisor_cleanup_authority:bootstrap_main"
liquent-supervisor-cleanup-authority-lifecycle = "liquent_platform.operators.manifest_handoff_supervisor_cleanup_authority:lifecycle_main"
liquent-supervisor-cleanup-authority-recovery = "liquent_platform.operators.manifest_handoff_supervisor_cleanup_authority:recovery_main"
liquent-supervisor-cleanup-source-revision = "liquent_platform.operators.manifest_handoff_supervisor_cleanup_source_revision:main"
liquent-supervisor-cleanup-retention-policy-bootstrap = "liquent_platform.operators.manifest_handoff_supervisor_cleanup_retention_policy:bootstrap_main"
liquent-supervisor-cleanup-retention-policy-change = "liquent_platform.operators.manifest_handoff_supervisor_cleanup_retention_policy:policy_main"
liquent-supervisor-cleanup-retention-authority-lifecycle = "liquent_platform.operators.manifest_handoff_supervisor_cleanup_retention_policy:lifecycle_main"
liquent-supervisor-cleanup-retention-authority-recovery = "liquent_platform.operators.manifest_handoff_supervisor_cleanup_retention_policy:recovery_main"
liquent-supervisor-cleanup-retention-evaluate = "liquent_platform.operators.manifest_handoff_supervisor_cleanup_retention_policy:retention_main"
liquent-supervisor-writer-wrapper = "liquent_platform.operators.manifest_handoff_supervisor_child:writer_main"
liquent-supervisor-recovery-wrapper = "liquent_platform.operators.manifest_handoff_supervisor_child:recovery_main"

[project.optional-dependencies]
dev = [
    "build>=1.3,<2",
    "pytest>=7.0",
    "setuptools>=80,<85",
    "wheel>=0.45,<1",
]
# LQ-020: optionales Visual-Preview-Extra. KEINE Pflicht-Dependency; nur für die
# lokale Streamlit-Preview unter tools/visual_preview/. dependencies=[] bleibt.
visual = ["streamlit>=1.0"]

[tool.setuptools.packages.find]
where = ["src", "."]
include = ["liquent*", "liquent_platform*", "tools*"]

[tool.setuptools.package-data]
liquent_platform = [
    "persistence/alembic/*.py",
    "persistence/alembic/*.mako",
    "persistence/alembic/versions/*.py",
]

[tool.pytest.ini_options]
pythonpath = ["src", "tests"]
testpaths = ["tests"]
markers = [
    "postgres_integration: needs a disposable PostgreSQL server (LQ-179)",
]
